name: sokode_core
description: Pure deterministic core for Sokode (grid state, rules, simulation).
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.5.0
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
